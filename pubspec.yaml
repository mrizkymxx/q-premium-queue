name: q_premium
description: "Q-PREMIUM — Sistem Manajemen Antrian Digital Kelas Premium"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  provider: ^6.1.0
  intl: ^0.19.0
  cupertino_icons: ^1.0.8
  web: ^1.1.1
  # google_fonts removed: loaded via CDN in web/index.html for web target
  flutter_animate: ^4.5.0
  qr_flutter: ^4.1.0
  flutter_tts: ^4.2.5
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
